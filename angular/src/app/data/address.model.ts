/**
 * Represents the _Address_ model
 *
 * ```yaml
 * id: string;
 * city: string;
 * country: string;
 * postalCode: string;
 * stateProvince: string;
 * street: string;
 * ```
 */
export class Address {
  id: string;
  city: string;
  country: string;
  postalCode: string;
  stateProvince: string;
  street: string;
}
