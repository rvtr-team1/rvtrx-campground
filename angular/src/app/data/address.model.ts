/**
 * Represents the _Address_ model
 *
 * ```yaml
 * id: string;
 * city: string;
 * country: string;
 * postalCode: string;
 * stateProvince: string;
 * street: string;
 * ```
 */
interface Address {
  id: string;
  city: string;
  country: string;
  postalCode: string;
  stateProvince: string;
  street: string;
}
