/**
 * Represents the _Link_ model
 *
 * ```yaml
 * icon: string;
 * text: string;
 * url: string;
 * ```
 */
export interface Link {
  /** icon path */
  icon: string;
  text: string;
  url: string;
}
