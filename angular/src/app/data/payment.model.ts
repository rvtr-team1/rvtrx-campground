/**
 * Represents the _Payment_ model
 *
 * ```yaml
 * id: string;
 * cardExpirationDate: string;
 * cardName: string;
 * cardNumber: string;
 * ```
 */
export class Payment {
  id: string;
  cardExpirationDate: string;
  cardName: string;
  cardNumber: string;
}
