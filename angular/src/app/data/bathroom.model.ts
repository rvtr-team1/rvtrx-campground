/**
 * Represents the _Bathroom_ model
 *
 * ```yaml
 * id: string;
 * fixture: number;
 * ```
 */
export interface Bathroom {
  id: string;
  /** number of bathrooms available */
  fixture: number;
}
