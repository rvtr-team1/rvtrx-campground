/**
 * Represents the _Review_ model
 *
 * ```yaml
 * id: string;
 * comment: string;
 * dateCreated: string;
 * rating: number;
 * ```
 */
export interface Review {
  id: string;
  comment: string;
  dateCreated: string;
  rating: number;
}
