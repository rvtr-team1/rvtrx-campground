/**
 * Represents the _Rental_ model
 *
 * ```yaml
 * id: string;
 * name: string;
 * occupancy: number;
 * type: string;
 * ```
 */
export interface Rental {
  id: string;
  name: string;
  occupancy: number;
  type: string;
}
