/**
 * Represents the _Profile_ model
 *
 * ```yaml
 * id: string;
 * email: string;
 * type: string;
 * givenName: string;
 * familyName: string;
 * phone: string;
 * ```
 */
export interface Profile {
  id: string;
  email: string;
  type: string;
  givenName: string;
  familyName: string;
  phone: string;
}
