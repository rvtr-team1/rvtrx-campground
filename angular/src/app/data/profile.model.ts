/**
 * Represents the _Profile_ model
 *
 * ```yaml
 * id: string;
 * email: string;
 * name: Name;
 * phone: string;
 * ```
 */
interface Profile {
  id: string;
  email: string;
  name: Name;
  phone: string;
}
